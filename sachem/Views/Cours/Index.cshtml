@model PagedList.IPagedList<Cours>
@using sachem.Models

@{
    ViewBag.Title = Messages.Cours;
}

<h2>@ViewBag.Title</h2>

@Html.ValidationSummary(true, "", new { @class = "text-danger" })
<p class="text-success">
    @Html.ViewBag.Success
    @TempData["Success"]
</p>

@using (Html.BeginForm("Index", "Cours", FormMethod.Post, new { @class = "form-inline", role = "form" }))
{
    <h4>@Messages.Recherche</h4>
    <table class="table-condensed">
        <tr>
            <td>@Messages.Session :</td>
            <td>@Html.DropDownList("Session", null, Messages.Toutes, new { @class = "form-search" })</td>
            <td>@Messages.Actif :</td>
            <td>@Html.CheckBox("Actif", (bool)ViewBag.Actif)</td>
            <td><input type="submit" value="@Messages.Rechercher" class="form-search" /></td>
        </tr>
    </table>
}

<div style="margin-top:5px;margin-bottom:10px;">@Html.ActionLink(Messages.CoursAjouterTitre, "Create", null, null, new { @class = "btn btn-default" })</div>

<table class="table">
    <tr>
        <th class="col-sm-1">
            @Html.DisplayNameFor(model => model.First().Code)
        </th>
        <th class="col-sm-8">
            @Html.DisplayNameFor(model => model.First().Nom)
        </th>
        <th class="col-sm-1">
            @Html.DisplayNameFor(model => model.First().Actif)
        </th>

        <th class="col-sm-2"></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Code)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Nom)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Actif)
            </td>

            <td>
                @Html.ActionLink(Messages.Modifier, "Edit", new { id = item.id_Cours }) |
                @Html.ActionLink(Messages.Supprimer, "Delete", new { id = item.id_Cours })
            </td>
        </tr>
    }

    @if (!Model.Any())
    {
        <tr>
            <td colspan="6">@Messages.AucunCours</td>
        </tr>
    }

</table>

@Html.Partial("_PageRecherche", Model)