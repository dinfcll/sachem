@using sachem.Models
@model PagedList.IPagedList<sachem.Models.Personne>
@{
    ViewBag.Title = "Index";
}

<h2>Étudiant</h2>
<h4>Recherche</h4>
@using (Html.BeginForm("Index", "Etudiant", FormMethod.Post, new { @class = "Recherche", role = "form" }))
{
<table class="Recherche">
    <tr>
        <th>
            @Html.DisplayName("Matricule:")
        </th>
        <th width = "4%">
            @Html.TextBox("SearchMatricule", (string)ViewBag.Mat)
        </th>
        <th>
            @Html.DisplayName("ou")
        </th>
        <th>
            @Html.DisplayName("Session :")
        @*@­<select>
            <option value="a"></option>
            <option value="b"></option>
            <option value="c"></option>
        </select>*@
            @Html.DropDownList("Session", "Toutes")
            @*@Html.DropDownList("Session", ViewBag.SessionList as SelectList)*@
            @*@Html.DropDownList("ListeCours", null, htmlAttributes:new {@class="form-control"})*@
        </th>
        <th>
            
        @Html.DisplayName("Cours: ")
        @*@Html.DropDownList("Cours", ViewBag.SessionList as SelectList)*@
        @Html.DropDownList("Cours", "Choisir")
        </th>
        <th>
            @Html.DisplayName("Groupe: ")
            @Html.DropDownList("Groupe")
            <input type="submit" value="Rechercher" class="btn-toolbar" />
                           
            @*<input type="button" value="Some text" onclick="window.location.href='<%= Url.Action("Rechercher", "EtudiantController" ) %>';" />*@ 
        </th>
    </tr>
</table>
}

    <p>
        @Html.ActionLink("Ajouter un étudiant", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayName("Nom")
            </th>
            <th>
                @Html.DisplayName("Matricule")
            </th>
            <th>
                @Html.DisplayName("Sexe")
            </th>
            <th>
                @Html.DisplayName("Date de naissance")
            </th>
            <th>
                @Html.DisplayName("Programme d'études")
            </th>

            <th></th>
        </tr>
        <BR>

        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Nom)
                    @Html.DisplayFor(modelItem => item.Prenom)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Matricule7)
                </td>

                <td>
                    @Html.DisplayFor(modelItem => item.p_Sexe.Sexe)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.DateNais,"{0:dd/MM/yyyy}")
                
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.ProgEtu)
                    @*@Html.DisplayFor(modelItem => item.DateNais)*@
                    @*@foreach (Personne personne in @Model)
                    {
                       @Html.DisplayFor(modelItem => item.Matricule7)
                    }*@
                </td>
                <td>
                    @Html.ActionLink("Modifier", "Edit", new { id = item.id_Pers }) |
                    @Html.ActionLink("Supprimer", "Delete", new { id = item.id_Pers })
                </td>
            </tr>
        }

    </table>
    @Html.Partial("_PageRecherche", Model)