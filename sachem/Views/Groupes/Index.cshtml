@model IEnumerable<sachem.Models.Groupe>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.NoGroupe)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Cours.Code)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Personne.Nom)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Session.id_Sess)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.NoGroupe)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Cours.Code)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Personne.Nom)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Session.id_Sess)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id_Groupe }) |
            @Html.ActionLink("Details", "Details", new { id=item.id_Groupe }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id_Groupe })
        </td>
    </tr>
}

</table>
